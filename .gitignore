# OS / editor cruft
.DS_Store
Thumbs.db
# FUSE mount artifacts (from the build sandbox)
.fuse_hidden*
# deps / logs / scratch
node_modules/
*.log

# local-only project notes and research (never published)
CLAUDE.local.md
82_0_research/

# Claude Code local config + worktrees (never published; repo root is the site)
.claude/

# Supabase CLI scratch (version cache, project ref) — regenerated, never ours to commit
supabase/.temp/
